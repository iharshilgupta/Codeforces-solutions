#include <bits/stdc++.h>
using namespace std;
auto speedup = []() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    return 0;
}();
int main() {
    vector<int> arr(4);
    for(int i=0;i<4;++i){
        cin>>arr[i];
    }
    int count=0;
    unordered_set<int> unique(arr.begin(),arr.end());
    count=4-unique.size();
    cout<<count<<endl;
    return 0;
}